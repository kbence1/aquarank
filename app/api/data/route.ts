import { NextResponse } from "next/server";import { addEvent,addResult,deleteResult,getEvents,placeBracketSlot,seedBracket } from "../../lib/data";
const name="aquarank_admin";async function token(){const secret=process.env.ADMIN_PASSWORD||"aquarank-demo";const b=await crypto.subtle.digest("SHA-256",new TextEncoder().encode(`aquarank:${secret}`));return Array.from(new Uint8Array(b)).map(x=>x.toString(16).padStart(2,"0")).join("");}function cookie(r:Request){return r.headers.get("cookie")?.split(";").map(x=>x.trim()).find(x=>x.startsWith(name+"="))?.slice(name.length+1);}async function allowed(r:Request){return cookie(r)===await token();}
export async function GET(){return NextResponse.json({events:await getEvents()},{headers:{"Cache-Control":"no-store"}});}
export async function POST(req:Request){const b=await req.json() as Record<string,unknown>;if(b.action==="login"){if(b.password!==(process.env.ADMIN_PASSWORD||"aquarank-demo"))return NextResponse.json({error:"Incorrect password"},{status:401});const res=NextResponse.json({ok:true});res.headers.append("Set-Cookie",`${name}=${await token()}; HttpOnly; Secure; SameSite=Strict; Path=/; Max-Age=28800`);return res;}if(!(await allowed(req)))return NextResponse.json({error:"Unauthorized"},{status:401});if(b.action==="addResult")await addResult({eventId:Number(b.eventId),athlete:String(b.athlete||""),club:String(b.club||""),time:String(b.time||"").replace(/\..*$/,"").slice(0,5),points:Number(b.points)});if(b.action==="addEvent")await addEvent({title:String(b.title||""),category:String(b.category||""),location:String(b.location||""),eventDate:String(b.eventDate||""),nextStart:String(b.nextStart||""),eventType:String(b.eventType||"standard"),bracketSize:Number(b.bracketSize||0)});if(b.action==="seedBracket")await seedBracket(Number(b.eventId),String(b.names||"").split(/\r?\n/));if(b.action==="placeBracketSlot")await placeBracketSlot(Number(b.eventId),Number(b.targetRound),Number(b.targetPosition),String(b.athlete||""));if(b.action==="deleteResult")await deleteResult(Number(b.id));return NextResponse.json({ok:true,events:await getEvents()});}
