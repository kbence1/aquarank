import { NextResponse } from "next/server";import { addEvent,addResult,deleteResult,getEvents,placeBracketSlot,seedBracket,setBracketStartTime,setOfficialStatus } from "../../lib/data";import{adminCookie,adminToken,isAdmin}from"../../lib/adminAuth";
export async function GET(){return NextResponse.json({events:await getEvents()},{headers:{"Cache-Control":"no-store"}});}
export async function POST(req:Request){const b=await req.json() as Record<string,unknown>;if(b.action==="login"){if(b.password!==(process.env.ADMIN_PASSWORD||"aquarank-demo"))return NextResponse.json({error:"Incorrect password"},{status:401});const res=NextResponse.json({ok:true});res.headers.append("Set-Cookie",adminCookie(await adminToken()));return res;}if(!(await isAdmin(req)))return NextResponse.json({error:"Unauthorized"},{status:401});if(b.action==="addResult")await addResult({eventId:Number(b.eventId),athlete:String(b.athlete||""),club:String(b.club||""),time:String(b.time||"").replace(/\..*$/,"").slice(0,5),points:Number(b.points)});if(b.action==="addEvent")await addEvent({title:String(b.title||""),category:String(b.category||""),location:String(b.location||""),eventDate:String(b.eventDate||""),nextStart:String(b.nextStart||""),eventType:String(b.eventType||"standard"),bracketSize:Number(b.bracketSize||0)});if(b.action==="seedBracket")await seedBracket(Number(b.eventId),String(b.names||"").split(/\r?\n/));if(b.action==="placeBracketSlot")await placeBracketSlot(Number(b.eventId),Number(b.targetRound),Number(b.targetPosition),String(b.athlete||""));if(b.action==="setBracketStartTime")await setBracketStartTime(Number(b.eventId),Number(b.round),Number(b.position),String(b.startTime||""));if(b.action==="setOfficialStatus")await setOfficialStatus(Number(b.eventId),Boolean(b.isOfficial));if(b.action==="deleteResult")await deleteResult(Number(b.id));return NextResponse.json({ok:true,events:await getEvents()});}
